# Application under test
BASE_URL=https://qa-practice.razvanvancea.ro

# Set by CI — adjusts workers and retries in playwright.config.ts
CI=false

# Ecommerce / login test credentials (never commit real values in .env)
ECOMMERCE_EMAIL=admin@admin.com
ECOMMERCE_PASSWORD=admin123
